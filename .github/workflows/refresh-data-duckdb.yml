# DuckDB data refresh, blue/green style. Parallel track to refresh-data.yml.
#
# Ships the *same* source data as the SQLite build, but converted to DuckDB
# (`python -m datasette_duckdb.convert`) and deployed to a SEPARATE Fly app
# (warehouse-duckdb) with its own machine + volume. The SQLite production app
# (warehouse / labordata.bunkum.us) is never touched by this workflow.
#
# Each run:
#   1. `make` builds the .db files from the same upstream sources
#   2. Converts each <name>.db -> <name>.duckdb (rm'ing the .db as it goes to
#      keep the runner under its disk cap)
#   3. Uploads the .duckdb files to R2 under the duckdb/ prefix
#   4. Builds the data-less warehouse-duckdb image (Dockerfile.duckdb) and
#      pushes it, pinning the resolved manifest digest
#   5. Creates a fresh volume + a role=staging machine on the freshly built
#      image (no public ports)
#   6. SFTPs pull-from-r2-direct.sh and populates /data from R2
#   7. Restarts to attach the files, smoke-tests on the private IP
#   8. Promotes: adds services to staging, cordons + destroys the old machine
#      and its volume (skipped harmlessly on the very first, bootstrap run)
#
# Unlike the SQLite track (which splits app deploys into deploy.yml and data
# into refresh-data.yml), this experimental track builds the image inside the
# refresh so a single workflow both bootstraps the app and refreshes data.
#
# Known gaps vs. the SQLite site (tracked in the datasette-duckdb work):
#   * No FTS yet — the converter doesn't build FTS indexes, so search boxes
#     are absent until that lands.
#   * No --crossdb / union_names canned queries (sqlite-specific).
#   * No inspect-data.json (`datasette inspect` is sqlite-shaped).

name: Refresh data (DuckDB)

on:
  # TEMPORARY: run on every push to this feature branch so we can exercise the
  # bootstrap end-to-end before merging to main (workflow_dispatch only works
  # for workflows on the default branch). Scoped to the branch so it never
  # fires on main, and the SQLite deploy.yml (which watches main) is untouched.
  # REMOVE this push: block before merging.
  push:
    branches:
      - duckdb-parallel-build
  schedule:
    # 30 min after the SQLite refresh (0 7) so upstream sources are settled
    # and the two runners don't contend.
    - cron: "30 7 * * *"
  workflow_dispatch:
    inputs:
      promote:
        description: "Promote staging to current after smoke (destroys current machine + volume)"
        type: boolean
        default: true

concurrency:
  # Own group: serialize DuckDB refreshes against each other, but don't block
  # on (or get blocked by) the SQLite warehouse-deploy group.
  group: warehouse-duckdb-deploy
  cancel-in-progress: false

permissions:
  contents: read

env:
  FLY_APP: warehouse-duckdb
  FLY_REGION: iad
  R2_BUCKET: labordata-warehouse-staging
  R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}
  DUCKDB_PREFIX: duckdb
  STAGING_VOL_GB: 10
  # TEMPORARY: push events promote too, so a commit-triggered bootstrap leaves a
  # running app + URL instead of a dry-run that tears itself down. Revert with
  # the push: trigger above.
  PROMOTE: ${{ (github.event_name == 'schedule' || github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.promote == true)) && 'true' || 'false' }}

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
                       /opt/hostedtoolcache/CodeQL || true
          df -h

      - name: Install build dependencies
        run: |
          # sqlite-utils / csvs-to-sqlite drive the Makefile; labor-union-parser
          # builds the crosswalk.
          pip install -r requirements.txt
          pip install labor-union-parser
          # The datasette-duckdb plugin brings the converter module + duckdb.
          pip install "datasette-duckdb @ git+https://github.com/fgregg/datasette@duckdb-backend#subdirectory=datasette-duckdb"
          # requirements.txt already installed a different datasette fork
          # (no_limit_csv), so pip treats the plugin's unpinned `datasette` dep
          # as satisfied and does NOT pull the backend-abstraction branch ->
          # `datasette.backends` is missing and convert.py fails on import.
          # Force the branch datasette last so it wins. `make` doesn't use
          # datasette, so replacing the no_limit_csv fork here is harmless.
          pip install --force-reinstall --no-deps "datasette @ git+https://github.com/fgregg/datasette@duckdb-backend"
          # Fail fast if the backends module still isn't importable.
          python -c "import datasette.backends, datasette_duckdb.convert; print('convert import OK')"

      - name: Build all databases
        run: make

      - name: Convert databases to DuckDB
        run: |
          # Convert one at a time and delete each .db immediately so peak disk
          # stays bounded (the .db set is ~10 GB; keeping both forms would blow
          # the runner). Fail loud if any conversion errors.
          set -eu
          for db in *.db; do
            name="${db%.db}"
            echo "::group::Converting $db -> $name.duckdb"
            python -m datasette_duckdb.convert "$db" "$name.duckdb"
            rm -f "$db"
            echo "::endgroup::"
          done
          ls -la *.duckdb
          df -h

      - name: Upload DuckDB databases to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL_S3: ${{ secrets.R2_ENDPOINT }}
          AWS_DEFAULT_REGION: auto
        run: |
          # Separate key namespace from the SQLite .db files in the same bucket.
          aws s3 sync . "s3://$R2_BUCKET/$DUCKDB_PREFIX/" \
            --exclude "*" --include "*.duckdb" \
            --no-progress

      - name: Install flyctl
        uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1  # v1.6

      - name: Ensure app + IPs exist
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Idempotent bootstrap. Create the app once (in the same org as the
          # existing `warehouse` app) and allocate shared IPs so the proxy can
          # route to it after promotion. Safe to run every time.
          if flyctl status --app "$FLY_APP" >/dev/null 2>&1; then
            echo "App $FLY_APP already exists"
          else
            ORG=$(flyctl apps list --json 2>/dev/null \
              | jq -r '[.[] | select(.Name=="warehouse") | .Organization.Slug] | first // empty')
            if [ -z "$ORG" ]; then
              echo "Could not infer org from the warehouse app; defaulting to 'personal'." >&2
              ORG=personal
            fi
            echo "Creating app $FLY_APP in org $ORG"
            flyctl apps create "$FLY_APP" --org "$ORG"
          fi
          # Allocate IPs (no-op if already allocated).
          flyctl ips allocate-v6 --app "$FLY_APP" 2>/dev/null || true
          flyctl ips allocate-v4 --shared --app "$FLY_APP" 2>/dev/null || true

      - name: Build image
        id: build
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Build-only + push, then pin by manifest digest (mirrors deploy.yml
          # on the SQLite track — bypasses Fly's tag->digest cache and lets us
          # roll the image with `machine run`, which never reconciles fly.toml
          # services and so never spawns data-less sibling machines).
          set -o pipefail
          TAG="build-$GITHUB_RUN_NUMBER"
          flyctl deploy --build-only --remote-only --app "$FLY_APP" \
            --config fly.duckdb.toml \
            --image-label "$TAG" \
            --push \
            --build-arg "GIT_SHA=$GITHUB_SHA" \
            2>&1 | tee /tmp/build.log
          DIGEST=$(grep -oE "pushing manifest for [^ ]*@sha256:[0-9a-f]+" /tmp/build.log \
                   | grep -oE "sha256:[0-9a-f]+" | tail -1)
          if [ -z "$DIGEST" ]; then
            echo "Could not extract manifest digest from build output." >&2
            exit 1
          fi
          echo "Resolved $TAG -> $DIGEST"
          echo "image=registry.fly.io/$FLY_APP:$TAG@$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Discover current machine + volume
        id: cur
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # On the bootstrap run there is no machine yet — that's fine, the
          # promote/destroy steps guard on empty. On later runs, prefer
          # role=current.
          JSON=$(flyctl machine list --app "$FLY_APP" --json)
          ROW=$(echo "$JSON" | jq '
            [.[] | select(.config.metadata.role == "current")][0] // .[0] // empty
          ')
          if [ -z "$ROW" ] || [ "$ROW" = "null" ]; then
            echo "No existing machine (bootstrap run)."
            echo "machine=" >> $GITHUB_OUTPUT
            echo "volume=" >> $GITHUB_OUTPUT
          else
            MID=$(echo "$ROW" | jq -r '.id')
            VOL=$(echo "$ROW" | jq -r '.config.mounts[0].volume // empty')
            echo "machine=$MID" >> $GITHUB_OUTPUT
            echo "volume=$VOL" >> $GITHUB_OUTPUT
            echo "Current: machine=$MID volume=$VOL"
          fi
          echo "Promote? $PROMOTE"

      - name: Create staging volume
        id: vol
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          NAME="dbs_stage_${GITHUB_RUN_ID}"
          VOL_ID=$(flyctl volumes create "$NAME" \
            --app "$FLY_APP" \
            --size "$STAGING_VOL_GB" \
            --region "$FLY_REGION" \
            --yes \
            --json \
            | jq -r '.id')
          echo "id=$VOL_ID" >> $GITHUB_OUTPUT
          echo "Created staging volume: $NAME ($VOL_ID)"

      - name: Create staging machine
        id: mach
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Built from the freshly pushed image (not from a current machine's
          # image), so this also works on the bootstrap run. No public ports
          # until promotion. `flyctl machine run` has no --json; parse stdout.
          OUT=$(flyctl machine run "${{ steps.build.outputs.image }}" \
            --app "$FLY_APP" \
            --region "$FLY_REGION" \
            --volume "${{ steps.vol.outputs.id }}:/data" \
            --metadata role=staging \
            --metadata refresh_run="$GITHUB_RUN_ID" \
            --vm-cpu-kind shared \
            --vm-cpus 1 \
            --vm-memory 2048 2>&1)
          echo "$OUT"
          MID=$(echo "$OUT" | grep -oE 'Machine ID: [a-f0-9]+' | head -1 | awk '{print $3}')
          if [ -z "$MID" ]; then
            echo "Could not parse machine ID from flyctl output" >&2
            exit 1
          fi
          echo "id=$MID" >> $GITHUB_OUTPUT
          echo "Created staging machine: $MID"
          sleep 15

      - name: Populate staging /data from R2
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # The .duckdb files are still on the runner (we only rm'd the .db
          # forms). Enumerate them for the pull list; the public base points at
          # the duckdb/ prefix we synced to. pull-from-r2-direct.sh cds /data
          # and writes each basename, so it's reused unchanged.
          ASSETS=$(ls *.duckdb | tr '\n' ' ')
          echo "Pulling: $ASSETS"
          flyctl ssh console --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}" \
            -C "rm -f /tmp/pull-from-r2-direct.sh"
          echo "put scripts/pull-from-r2-direct.sh /tmp/pull-from-r2-direct.sh" \
            | flyctl ssh sftp shell --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}"
          flyctl ssh console --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}" \
            -C "sh /tmp/pull-from-r2-direct.sh $R2_PUBLIC_BASE/$DUCKDB_PREFIX $ASSETS"

      - name: Restart staging to attach fresh data
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          MID="${{ steps.mach.outputs.id }}"
          flyctl machine restart "$MID" --app "$FLY_APP"
          for i in $(seq 1 90); do
            state=$(flyctl machine list --app "$FLY_APP" --json \
              | jq -r --arg id "$MID" '.[] | select(.id == $id) | .state')
            echo "  attempt $i: state=$state"
            if [ "$state" = "started" ]; then break; fi
            sleep 2
          done
          if [ "$state" != "started" ]; then
            echo "Machine never reached started state" >&2
            exit 1
          fi

      - name: Wait for SSH (hallpass) on staging
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          MID="${{ steps.mach.outputs.id }}"
          for i in $(seq 1 30); do
            if flyctl ssh console --app "$FLY_APP" --machine "$MID" \
                 -C "true" >/dev/null 2>&1; then
              echo "  ssh ready on attempt $i"
              break
            fi
            echo "  attempt $i: ssh not ready"
            sleep 2
          done
          flyctl ssh console --app "$FLY_APP" --machine "$MID" -C "true"

      - name: Smoke test datasette on staging
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Reused unchanged: datasette names a db by its filename stem, so
          # <name>.duckdb attaches as "<name>" — the same set smoke-test.sh
          # expects.
          flyctl ssh console --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}" \
            -C "rm -f /tmp/smoke-test.sh"
          echo "put scripts/smoke-test.sh /tmp/smoke-test.sh" \
            | flyctl ssh sftp shell --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}"
          flyctl ssh console --app "$FLY_APP" --machine "${{ steps.mach.outputs.id }}" \
            -C "sh /tmp/smoke-test.sh"

      # ─── Promote path (only when explicitly requested) ──────────────

      - name: Promote staging to current
        if: env.PROMOTE == 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          set -eu
          NEW="${{ steps.mach.outputs.id }}"
          OLD="${{ steps.cur.outputs.machine }}"

          # 1. Put services on the staging machine so Fly's proxy routes to it.
          flyctl machine update "$NEW" --app "$FLY_APP" --yes \
            --port "80:8080/tcp:http" \
            --port "443:8080/tcp:tls:http"

          # 2/3. If there's an old machine (i.e. not the bootstrap run), cordon
          #      it and let connections drain before swapping roles.
          if [ -n "$OLD" ] && [ "$OLD" != "null" ]; then
            flyctl machine cordon "$OLD" --app "$FLY_APP"
            sleep 45
          fi

          # 4. Promote metadata so the next run finds this machine.
          flyctl machine update "$NEW" --app "$FLY_APP" --yes \
            --metadata role=current

      - name: Destroy old current
        if: env.PROMOTE == 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          OLD="${{ steps.cur.outputs.machine }}"
          OLDVOL="${{ steps.cur.outputs.volume }}"
          if [ -n "$OLD" ] && [ "$OLD" != "null" ]; then
            flyctl machine destroy "$OLD" --app "$FLY_APP" --force
          fi
          if [ -n "$OLDVOL" ] && [ "$OLDVOL" != "null" ]; then
            flyctl volumes destroy "$OLDVOL" -y
          fi

      # ─── Teardown of staging if not promoted (dry-run + failure path) ─

      - name: Tear down staging
        if: always() && (env.PROMOTE != 'true' || failure())
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          if [ -n "${{ steps.mach.outputs.id }}" ]; then
            flyctl machine destroy "${{ steps.mach.outputs.id }}" \
              --app "$FLY_APP" --force || true
          fi
          if [ -n "${{ steps.vol.outputs.id }}" ]; then
            flyctl volumes destroy "${{ steps.vol.outputs.id }}" -y || true
          fi
