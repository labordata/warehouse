# Data-only refresh.
#
# Rebuilds the SQLite databases from upstream sources, uploads them to
# R2, has the warehouse machine pull them in-network, swaps them onto
# the volume, then RESTARTS the machine to pick them up — no image
# redeploy, no new image build.
#
# Triggers: daily cron after upstream data sources update + manual
# dispatch. Shares a concurrency group with `deploy.yml` so a deploy
# and a refresh never race.

name: Refresh data

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

concurrency:
  group: warehouse-deploy
  cancel-in-progress: false

env:
  FLY_APP: warehouse
  R2_BUCKET: labordata-warehouse-staging
  R2_PUBLIC_BASE: ${{ secrets.R2_PUBLIC_BASE }}

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
                       /opt/hostedtoolcache/CodeQL || true
          df -h

      - name: Install build dependencies
        run: |
          pip install -r requirements.txt
          pip install labor-union-parser

      - name: Build all databases
        run: make

      - name: Build inspect-data.json
        run: |
          # Pre-compute per-table row counts + schema hashes so datasette
          # doesn't have to do it at startup on every machine restart.
          datasette inspect *.db > inspect-data.json
          ls -lh inspect-data.json

      - name: Upload databases to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL_S3: ${{ secrets.R2_ENDPOINT }}
          AWS_DEFAULT_REGION: auto
        run: |
          aws s3 sync . "s3://$R2_BUCKET/nightly/" \
            --exclude "*" \
            --include "*.db" \
            --include "inspect-data.json" \
            --no-progress

      - name: Install flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Ensure machine is running before pull
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          flyctl machine list --app "$FLY_APP" --json \
            | jq -r '.[].id' \
            | xargs -I{} flyctl machine start {} --app "$FLY_APP" || true
          sleep 10

      - name: Pull databases from R2 to Fly volume
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          ASSETS=$(python -c "import json; print(' '.join(k+'.db' for k in json.load(open('inspect-data.json')).keys()))")
          ASSETS="$ASSETS inspect-data.json"
          echo "Pulling: $ASSETS"
          # `flyctl ssh sftp put` refuses to overwrite, so clear any prior
          # copy before SFTPing the helper into /tmp.
          flyctl ssh console --app "$FLY_APP" -C "rm -f /tmp/pull-from-r2.sh"
          echo "put scripts/pull-from-r2.sh /tmp/pull-from-r2.sh" \
            | flyctl ssh sftp shell --app "$FLY_APP"
          flyctl ssh console --app "$FLY_APP" -C \
            "sh /tmp/pull-from-r2.sh $R2_PUBLIC_BASE/nightly $ASSETS"

      - name: Swap databases atomically
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          flyctl ssh console --app "$FLY_APP" -C '/app/scripts/swap-data.sh'

      - name: Restart datasette to load new data
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Data refresh, not a code change — bounce the machine to
          # re-mmap the new files. `flyctl machine restart` skips the
          # image push that `flyctl deploy` would do.
          flyctl machine list --app "$FLY_APP" --json \
            | jq -r '.[].id' \
            | xargs -I{} flyctl machine restart {} --app "$FLY_APP"

      - name: Purge Cloudflare cache
        env:
          CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: |
          response=$(curl -fsS -X POST \
            "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/purge_cache" \
            -H "Authorization: Bearer ${CF_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}')
          echo "$response"
          echo "$response" | grep -q '"success":true' || { echo "Purge failed"; exit 1; }
